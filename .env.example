# Resend (transactional email). Provision via Vercel Marketplace or get a key at https://resend.com
RESEND_API_KEY=
ALERT_TO_EMAIL=kdalene@gmail.com
ALERT_FROM_EMAIL=onboarding@resend.dev

# Upstash Redis (KV for dedup + signal log).
# Vercel Marketplace auto-provisions these names:
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Or, if you signed up directly at upstash.com:
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Random 32+ char secret. Vercel Cron sends this as Authorization: Bearer ${CRON_SECRET}
CRON_SECRET=
